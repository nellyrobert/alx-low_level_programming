#include "main.h"
#include <string.h>
/**
 *_strlen - main function
 *@s : our string pointer variable
 *
 *Return: print lenght of the string
 */

int _strlen(char *s)
{
int i;
for (i = 0; s[i] != '\0'; i++)
{
continue;
}
return (i);
}
